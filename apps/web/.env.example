# Base URL of the CMS (apps/cms), used at build time to fetch content. No trailing slash
CMS_URL=http://localhost:3000

# Public URL of this web site, used for canonical URLs, sitemap and Open Graph. No trailing slash
NEXT_PUBLIC_SITE_URL=http://localhost:3001
